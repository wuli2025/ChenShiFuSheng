node_modules
dist
dist-ssr
*.local
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
src-tauri/target
src-tauri/gen

# Runtime outputs / local logs
.dev.log
output/
PolarisKB/output/
# 对话产物（运行时生成，落在工作文件夹/KB 根下的 conversations/，不该进仓库）
PolarisKB/conversations/
conversations/

# Claude workspace / temp files
.claude/
*.html
!index.html
!src-tauri/**/*.html

# 本地开发产物 / 截图 / 日志（不进仓库）
.shot-*.png
.dev-screenshot-*.png
.dev-server.log
.dev-run.log
/workspace/
PolarisKB/.polaris_seeded

# 本地工作/产物草稿目录（运行时生成，不进仓库）
/career-map-work/
/polaris-video-work/
/polaris-video-work-kbwiki/

# 生成的视频工作产物(大体积,勿提交)
/polaris-video-work-longoutput/
/polaris-video-work-zhiyuan/
/polaris-video-work-kbwiki/

# Docker 化本地产物（勿提交）
.claude-seed/
.docker-build.log
.docker-monitor.log
.docker-monitor-dry.log
docker-compose.override.yml

# UI 预览构建产物（并行会话生成,8GB+,勿提交）
src-tauri/target-ui-preview/

__pycache__/
*.pyc

# 本地运行日志 / 临时脚本草稿（运行时生成,勿提交）
_scratch/
src-tauri/*.log

# 环境变量（含 API 密钥，绝不入库；保留示例模板）
.env
.env.local

# 本地 dev 运行日志（根目录）
_run_dev.log
_run_dev.err.log

# 外部 HTML 游戏需入库——覆盖上面的 *.html 忽略规则
!public/external-games/**

# 个人知识库数据（不入公开仓库；只传代码）
/PolarisKB/

# 打包用 claude 自带二进制(234MB, 构建期暂存, 不入库)
src-tauri/resources/claude-bin/
src-tauri/tauri.localbuild.conf.json

# codex 出图隔离 home(含 auth.json 令牌副本,勿提交)
scripts/codex/homes/
scripts/codex/gen-*.log
scripts/codex/gen.log

# 临时开发/构建日志(勿提交)
_build_installer.log
_dev*.log
.env.prod

# v11 refactor
backend/target/
_legacy/**/node_modules/

node_modules/
backend/target/
dist/

node_modules
dist/
.env
